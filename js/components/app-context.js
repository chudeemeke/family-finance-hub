// Component stubs - all components are defined in app.js
// These files exist to prevent 404 errors when index.html tries to load them

// Export empty object to prevent errors
window.AppContext = window.AppContext || {};